<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="GridLayoutStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_centerHorizontal">true</item>
        <item name="alignmentMode">alignBounds</item>
        <item name="columnCount">3</item>
    </style>

    <style name="RubberButtonInGreedLayout" parent="android:Widget.Button">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:text">some text</item>
        <item name="layout_columnWeight">10</item>
        <item name="layout_gravity">fill</item>
        <item name="layout_rowWeight">10</item>
    </style>

    <style name="RubberActionButton" parent="android:Widget.ImageButton">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">0dp</item>
        <item name="layout_columnWeight">1</item>
        <item name="layout_gravity">fill</item>
        <item name="layout_rowWeight">3</item>
    </style>

    <style name="SecondActivityStyleText" parent="android:Widget.TextView">
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_width">match_parent</item>
    </style>

</resources>
